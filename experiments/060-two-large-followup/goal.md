# Experiment 060 goal

Continue the same two-large-shape campaign from exact ranked incumbent
`#904546`. Integrate only independently verified incremental changes for
`1×16384` and `1×32768`, then promote through paired target, six-family,
full-grid, cold-build, Popcorn test, and serialized leaderboard gates.

The campaign target remains at least a 10% cumulative leaderboard geomean
reduction relative to ranked `#890798`.
