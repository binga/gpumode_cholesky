# Experiment 059 — two-large-shape incremental integration

Integrate the already measured blocked triangular-inverse frontier only for
`batch=1,n=16384` and `batch=1,n=32768` from ranked `#890798`.

This is an incremental checkpoint toward the campaign's 10% aggregate target.
It may be promoted only after exact-source family, full-grid paired, cold-build,
and Popcorn 17/17 gates. Exactly one ranked submission may run at a time.
