__pycache__/
*.pyc
.venv/
.env
# Modal / popcorn local config and raw run logs
.modal.toml
# Keep committed JSON summaries under results/, ignore raw run dumps
results/*
!results/*.json
# macOS junk
.DS_Store
# Agent scratch: .claude/ holds git worktrees (recursive repo copies, ~400M)
.claude/
.codex/
*.bak
